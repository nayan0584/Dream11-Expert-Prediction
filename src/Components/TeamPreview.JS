import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const TeamPreview = () => {
  return (
    <View>
      <Text>TeamPreview</Text>
    </View>
  );
};

export default TeamPreview;

const styles = StyleSheet.create({});
